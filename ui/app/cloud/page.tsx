'use client';

import { Header } from '@/lib/Header';
import { LayoutMain } from '@/lib/Layout';

export default function CloudSetup() {
  return (
    <LayoutMain alignSelf='center' justifySelf='center' width='xxLarge'>
      <Header variant='largeTitle'>Cloud</Header>
    </LayoutMain>
  );
}
